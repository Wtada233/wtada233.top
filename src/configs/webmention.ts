import type { WebmentionConfig } from "@/types/config";

export const webmentionConfig: WebmentionConfig = {
	enable: true,
	endpoint: "https://webmention.io/wtada233.top/webmention", // Corrected to standard webmention endpoint
};
